export const showMenuBtn: { [key: string]: boolean } = {
  'energy': true,
  'config/dashboard': true,
  'todo': true,
  'developer-tools/yaml': true,
}

export const allowedPages = Object.keys(showMenuBtn);
